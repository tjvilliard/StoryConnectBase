import 'package:flutter/material.dart';

class WritingAppView extends StatelessWidget {
  const WritingAppView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Writing App'),
      ),
    );
  }
}
